package handlers

// CommandHandler endpoint will essentially act as a gateway to other applications.
// It will accept a data payload with a type property and respond appropriately if the type of payload is known, otherwill will not do anything.
